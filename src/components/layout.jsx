import { NavLink, useLocation } from 'react-router-dom'
import { Bell, ClipboardList, Leaf, LogOut, PackageSearch, ShoppingBasket } from 'lucide-react'
const links = [{ to: '/pantry', label: 'Pantry', icon: PackageSearch }, { to: '/purchases', label: 'Purchases', icon: ClipboardList }, { to: '/reminders', label: 'Reminders', icon: Bell }, { to: '/shopping-list', label: 'List', icon: ShoppingBasket }]
function DesktopNav() { return <nav className="hidden items-center gap-1 md:flex" aria-label="Main navigation">{links.map(({ to, label }) => <NavLink key={to} to={to} className={({ isActive }) => `rounded-full px-4 py-2 text-sm transition ${isActive ? 'bg-[#e9f1df] font-medium text-[#314522]' : 'text-slate-500 hover:bg-slate-100 hover:text-slate-900'}`}>{label}</NavLink>)}</nav> }
function MobileNav() { return <nav className="grid h-full grid-cols-4" aria-label="Main navigation">{links.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} className={({ isActive }) => `flex flex-col items-center justify-center gap-1 text-[10px] font-medium ${isActive ? 'text-[#49752c]' : 'text-slate-400'}`}><Icon size={19} strokeWidth={2} /><span>{label}</span></NavLink>)}</nav> }
export function AppLayout({ children, user, onLogout }) { const location = useLocation(); const title = links.find(link => location.pathname.startsWith(link.to))?.label || 'Pantry'; const initial = user?.email?.[0]?.toUpperCase() || 'G'; return <div className="min-h-screen"><header className="sticky top-0 z-30 border-b border-slate-200/80 bg-[#fafbf8]/90 backdrop-blur"><div className="mx-auto flex h-16 max-w-[1280px] items-center justify-between px-4 sm:px-7 lg:px-10"><NavLink to="/pantry" className="flex items-center gap-2.5"><span className="grid h-8 w-8 place-items-center rounded-xl bg-[#1b2514] text-[#c6e395]"><Leaf size={17} /></span><span className="font-semibold tracking-[-.03em] text-[#171f11]">Pantry</span></NavLink><DesktopNav/><div className="hidden items-center gap-2 md:flex"><span className="max-w-40 truncate text-sm text-slate-500">{user?.email || 'Demo mode'}</span><div className="grid h-8 w-8 place-items-center rounded-full bg-[#e9f1df] text-xs font-semibold text-[#314522]" aria-label="Profile">{initial}</div>{onLogout && <button type="button" onClick={onLogout} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 hover:text-slate-900" aria-label="Log out"><LogOut size={17} /></button>}</div><div className="flex items-center gap-1 md:hidden"><span className="text-sm font-medium text-slate-500">{title}</span>{onLogout && <button type="button" onClick={onLogout} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 hover:text-slate-900" aria-label="Log out"><LogOut size={17} /></button>}</div></div></header><main className="mx-auto min-h-screen w-full max-w-[1280px] px-4 pb-24 pt-8 sm:px-7 lg:px-10 lg:pb-12 lg:pt-12">{children}</main><div className="fixed bottom-0 z-30 h-[70px] w-full border-t border-slate-200/80 bg-white/95 backdrop-blur md:hidden"><MobileNav /></div></div> }
